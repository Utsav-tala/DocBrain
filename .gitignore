# Environment
.env
.venv/
venv/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/

# Vector DB (large binary — regenerate via ingest.py)
db/chroma_langchain/

# Data (large raw doc dumps — regenerate via ingest.py)
data/langchain_conceptual_docs/
data/langchain_codebase/
data/langchain_issues.json

# OS / Editor / tooling
.DS_Store
.vscode/
.idea/
.claude/

# Streamlit
.streamlit/secrets.toml

# NOTE: eval_data/ (golden set) and results/ (eval numbers) are intentionally
# TRACKED — they back the evaluation claims in the README.

# Personal notes & planning docs (local only — not for the public repo)
notes/